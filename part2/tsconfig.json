{
  // 对 ts 编译器进行配置 
  // 需要编译的文件目录
  // ** 表示任意目录  * 表示任意文件
  "include":[
    "./src/**/*"
  ],
  // 不需要编译的文件目录
  "exclude":[
    "./src/exclude/**/*"
  ],
  "compilerOptions": {
    "target":"ES3"
  }
}
